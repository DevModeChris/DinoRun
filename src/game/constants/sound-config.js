/**
 * 🎵 Sound configuration constants
 * Controls how sounds behave in our game
 */
export const SoundConfig = {
    /** Pitch range for jump sounds (0.8 to 1.3 for subtle variation) */
    JUMP_PITCH_RANGE: { MIN: 0.8, MAX: 1.3 },

    /** Pitch range for landing sounds (slightly less variation) */
    LAND_PITCH_RANGE: { MIN: 0.95, MAX: 1.05 },
};
